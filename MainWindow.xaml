<Window x:Class="Labs.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="TextEditor" Height="300" Width="500">

    <!-- РЕСУРСИ -->
    <Window.Resources>
        <LinearGradientBrush x:Key="LinearPanelBrush" 
                     StartPoint="0,0.5" 
                     EndPoint="1,0.5">

            <GradientStop Color="Yellow" Offset="0.0"/>
            <GradientStop Color="Turquoise"     Offset="0.5"/>
            <GradientStop Color="Yellow" Offset="1.0"/>
        </LinearGradientBrush>

        <Style x:Key="ButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="Transparent"/>
        </Style>

    </Window.Resources>

    <Border Margin="5"
            CornerRadius="10"
           Background="{StaticResource LinearPanelBrush}"
            BorderThickness="2">

        <Grid>

            <Grid.RowDefinitions>
                <RowDefinition Height="60"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="40"/>
            </Grid.RowDefinitions>

            <StackPanel Orientation="Horizontal"
                        HorizontalAlignment="Left"
                        Margin="5">
                <Button Width="50" Style="{StaticResource ButtonStyle}">
                    <Image Source="Image/open_img.png"/>
                </Button>
                <Button Width="50" Style="{StaticResource ButtonStyle}">
                    <Image Source="Image/delete_img.png"/>
                </Button>
                <Button Width="50" Style="{StaticResource ButtonStyle}">
                    <Image Source="Image/copy_img.png"/>
                </Button>
                <Button Width="50" Style="{StaticResource ButtonStyle}">
                    <Image Source="Image/paste_img.png"/>
                </Button>

            </StackPanel>

            <Border Grid.Row="1"
                    Margin="10"
                    CornerRadius="5"
                    BorderBrush="#CBBEBE"
                    BorderThickness="2">

                <ScrollViewer>
                    <TextBlock Background="LightGray" Text="Text" FontSize="{Binding ElementName=fontSlider, Path=Value}"/>
                </ScrollViewer>
            </Border>

            <DockPanel Grid.Row="2"
                       HorizontalAlignment="Left"
                       VerticalAlignment="Center">

                <Label Content="Розмір шрифту:" />
                <Slider Margin="4" MinWidth="100" Name="fontSlider" Maximum="100" Minimum="12" />

            </DockPanel>
            <Button
                Content="OK"
                HorizontalAlignment="Left"
                VerticalAlignment="Top"
                Style="{x:Null}" RenderTransformOrigin="3.701,0.54" Margin="307,7,0,0" Grid.Row="2"/>
        </Grid>
    </Border>
</Window>