<Window x:Class="Labs.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="TextEditor" Height="300" Width="500">

    <!-- РЕСУРСИ -->
    <Window.Resources>
        <LinearGradientBrush x:Key="LinearPanelBrush" 
                     StartPoint="0,0.5" 
                     EndPoint="1,0.5">

            <GradientStop Color="Yellow" Offset="0.0"/>
            <GradientStop Color="Turquoise"     Offset="0.5"/>
            <GradientStop Color="Yellow" Offset="1.0"/>
        </LinearGradientBrush>

        <Style x:Key="ButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="Transparent"/>
        </Style>

    </Window.Resources>

    <Border Margin="5"
            CornerRadius="10"
           Background="{StaticResource LinearPanelBrush}"
            BorderThickness="2">

        <Grid>

            <Grid.RowDefinitions>
                <RowDefinition Height="60"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="40"/>
            </Grid.RowDefinitions>

            <StackPanel Orientation="Horizontal"
                        HorizontalAlignment="Left"
                        Margin="5">
                <Button Width="50" Style="{StaticResource ButtonStyle}">
                    <Image Source="Image/open_img.png"/>
                </Button>
                <Button Width="50" Style="{StaticResource ButtonStyle}">
                    <Image Source="Image/delete_img.png"/>
                </Button>
                <Button Width="50" Style="{StaticResource ButtonStyle}">
                    <Image Source="Image/copy_img.png"/>
                </Button>
                <Button Width="50" Style="{StaticResource ButtonStyle}">
                    <Image Source="Image/paste_img.png"/>
                </Button>

            </StackPanel>

            <Border Grid.Row="1"
                    Margin="10"
                    CornerRadius="5"
                    BorderBrush="#CBBEBE"
                    BorderThickness="2">

                <ScrollViewer>
                    <TextBox Background="LightGray" BorderThickness="0"/>
                </ScrollViewer>
            </Border>
            <Button Grid.Row="2"
                    Content="OK"
                    HorizontalAlignment="Center"
                    VerticalAlignment="Center"
                    Style="{x:Null}"/>
        </Grid>
    </Border>
</Window>